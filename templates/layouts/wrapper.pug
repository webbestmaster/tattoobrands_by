doctype html
html(lang="ru")

    head

        meta(charset="utf-8")
        meta(name="format-detection" content="telephone=no")
        meta(name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0")
        meta(http-equiv="X-UA-Compatible" content="IE=edge")
        if keywords !== ''
            meta(name="keywords" content=keywords)
        meta(name="description" content=description)
        title=title

        link(href="/front/style.css" rel="stylesheet")

        //style(rel="stylesheet")
        //    include ./../../public/front/style.css

        //Global site tag (gtag.js) - Google Analytics
        script.
            (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
                (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
                m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
            })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

            ga('create', 'UA-135378491-1', 'auto');
            ga('require', 'displayfeatures');
            ga('send', 'pageview');

        //Yandex.Metrika counter
        //script(src="/front/yandexMetrika.js" type="text/javascript")
        script.
            (function (m, e, t, r, i, k, a) {
                m[i] = m[i] || function () {
                    (m[i].a = m[i].a || []).push(arguments)
                }
                m[i].l = 1 * new Date();
                k = e.createElement(t), a = e.getElementsByTagName(t)[0], k.async = 1, k.src = r, a.parentNode.insertBefore(k, a)
             })
             (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym")

             ym(52584814, "init", {
                 id: 52584814,
                 clickmap: true,
                 trackLinks: true,
                 accurateTrackBounce: true,
                 webvisor: true
             })  

        noscript
            div
                img(src="https://mc.yandex.ru/watch/52584814" style="position:absolute; left:-9999px;")


    body

        div.main-wrapper
            include ./header
            include ./header-nav

            block content

        include ./footer

        script
            != 'window.app = window.app || {};'
            != 'window.app.categoryTree = ' + JSON.stringify(categoryTree) + ';'

        script(src="/front/common.js" type="text/javascript" defer)
        script(src="/front/main.js" type="text/javascript" defer)
